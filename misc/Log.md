
## April 29, 2026 

### What I did 

- Installed Go 
- Set up GitHub repo 
- Created initial Go program 

### What I learned 

- How to run Go programs using `go run` 
- Difference between `Print`, `Println`, `Printf` 

### Problems 

- GitHub authentication failed 
- Token confusion 

### Fixes

- Created personal access token 
- Used token instead of password

##  May 6, 2026

### What I did

- Learned how to use Go’s `net` package for networking
- Used `net.LookupIP()` to resolve hostnames into IP addresses
- Used `net.Dial()` to create TCP connections
- Experimented with `time.Now()` and `time.Since()` for latency measurements
- Tested DNS lookup timing and connection timing separately

### What I learned

- Difference between DNS resolution and TCP connection setup
- How Go measures elapsed time using `time.Since()`
- Why TCP connections require both a hostname and port
- How monotonic clocks are used internally for accurate timing
- Basic networking workflow:
  - resolve hostname
  - connect to host
  - measure latency

### Problems

- `net.Dial()` failed when no port was included
- User input contained newline characters (`\n`)
- Timer placement initially measured the wrong parts of the program
- Program risked crashing when `conn.Close()` was called after failed connections

### Fixes

- Added ports using:
  ```go
  address := host + ":80"

##  May 7, 2026

### What I did

- Studied the core concepts behind traceroute and ICMP networking
- Read articles explaining:
  - TTL (Time To Live)
  - ICMP Echo Requests and Replies
  - ICMP Time Exceeded messages
  - how routers handle packets
  - how traceroute discovers intermediate hops
- Ran example traceroute-related Go code using:
  - `golang.org/x/net/icmp`
  - `golang.org/x/net/ipv4`
- Initialized a Go module and installed external networking packages
- Used `sudo` to allow raw ICMP socket access on macOS
- Analyzed and stepped through multiple traceroute code snippets line-by-line

### Terms / Concepts Studied

- TTL (Time To Live)
- ICMP
- ICMP Echo Request
- ICMP Echo Reply
- ICMP Time Exceeded
- Raw Sockets
- MTU (Maximum Transmission Unit)

### What I learned

- How traceroute works internally by increasing TTL values
- Why routers send ICMP Time Exceeded responses when TTL reaches zero
- Difference between application-level networking and packet-level networking
- How ICMP packets are created and marshaled into raw bytes
- How `WriteTo()` sends raw ICMP packets to destination hosts
- How `ReadFrom()` listens for router and destination responses
- How traceroute determines whether a response came from:
  - an intermediate router
  - or the final destination
- Why raw ICMP sockets require administrator/root privileges

### Problems

- Go module dependencies were missing
- `go.mod` and `go.sum` issues occurred
- Program was not using `package main`
- Raw ICMP socket creation failed with:
  - `operation not permitted`

### Fixes

- Initialized the project using:
  ```bash
  go mod init traceroute-project

## May 13, 2026

### What I did

- Installed and configured Wireshark on macOS
- Fixed Wireshark packet capture permissions using ChmodBPF
- Captured live network traffic using Wireshark
- Applied ICMP packet filters inside Wireshark
- Ran:
  - `ping`
  - traceroute-related Go programs
- Observed ICMP Echo Requests and Echo Replies
- Observed ICMP Time Exceeded packets generated by routers
- Learned how traceroute uses TTL values to discover routers hop-by-hop
- Expanded and inspected IPv4 and ICMP headers inside Wireshark
- Studied how TTL values appear in packet headers
- Compared:
  - ICMP Echo Reply
  - ICMP Time Exceeded
  - ICMP Echo Request
- Used:
  - `nslookup`
  - `dig`
- Compared DNS lookups with Go’s `net.LookupIP()`
- Read documentation and example code related to:
  - ICMP
  - TTL manipulation
  - raw sockets
  - traceroute logic
- Analyzed example traceroute code snippets and broke down how they work internally

### Terms / Concepts Studied

- ICMP
- ICMP Echo Request
- ICMP Echo Reply
- ICMP Time Exceeded
- TTL (Time To Live)
- Raw Sockets
- MTU (Maximum Transmission Unit)
- DNS
- Latency

### What I learned

- How traceroute works internally by increasing TTL values
- Why routers send ICMP Time Exceeded responses
- Difference between DNS TTL and packet TTL
- How ICMP packets appear inside Wireshark
- How routers and destination hosts respond differently
- How raw ICMP sockets work
- How packet captures relate to the traceroute code
- Difference between TCP networking and ICMP packet networking
- How Wireshark can be used to analyze real network traffic

### Problems

- Wireshark initially lacked packet capture permissions
- Difficulty locating TTL values inside packet headers
- Confusion between sequence numbers and TTL values
- Initially overwhelmed by the amount of traffic displayed in Wireshark

### Fixes

- Installed ChmodBPF permissions for Wireshark
- Applied ICMP filters to reduce unrelated traffic
- Focused specifically on:
  - IPv4 headers
  - ICMP sections
  - TTL fields
  - ICMP response types

## May 14, 2026

### What I did

- Continued improving the TCP-based networking tool in Go
- Added multiple TCP connection attempts using a `for` loop
- Began implementing average connection latency calculations
- Added connection timing measurements for multiple probes
- Learned how to use `net.DialTimeout()` for timeout handling
- Compared timeout handling in:
  - TCP networking
  - raw ICMP networking
- Learned more about Go loops, variable scope, and `time.Duration`
- Studied how repeated probes work in networking tools such as ping and traceroute
- Learned the basics of Go goroutines and `sync.WaitGroup`
- Organized the future traceroute roadmap into:
  - improving the TCP tool
  - ICMP concepts
  - TTL manipulation
  - full traceroute implementation
  - concurrency improvements

### What I learned

- Why `conn.Close()` should usually be used instead of `defer conn.Close()` inside loops
- How average latency calculations work
- Difference between:
  - connection latency
  - total probe time
  - DNS lookup time
- How Go handles multiple return values using:
  ```go
  conn, err :=
  ```
- How timeouts prevent networking programs from hanging
- Why networking tools often perform multiple probes instead of a single test
- Difference between sequential probing and concurrent probing

### Problems

- Average latency calculations were resetting incorrectly
- Connection attempts failed due to malformed address strings
- Connections stayed open longer than intended because of `defer conn.Close()` inside loops
- Initial confusion about where timeout logic should be implemented

### Fixes

- Moved total latency variables outside the loop
- Replaced deferred connection closing with:
  ```go
  conn.Close()
  ```
- Corrected hostname formatting issues using:
  ```go
  strings.TrimSpace()
  ```
- Implemented timeout handling using:
  ```go
  net.DialTimeout()
  ```
- Placed timeout handling inside the probe loop so each connection attempt has its own timeout

## May 17, 2026

### What I did

- Organized the Go traceroute project structure
- Created folders for:
  - traceroute logic
  - latency testing
  - DNS functionality
  - utilities
  - logs/documentation
- Planned how networking features will be separated into packages and files

### What I learned

- Go projects are easier to manage when functionality is separated into packages
- Project structure becomes more important as networking tools grow in complexity

### Next Steps

- Begin implementing traceroute functionality
- Continue improving project organization and logging

## May 18, 2026

### What I did

- Began implementing a raw ICMP ping tool in Go
- Created ICMP Echo Request packets using:
  - `icmp.Message`
  - `icmp.Echo`
- Used:
  - `icmp.ListenPacket()`
  - `WriteTo()`
  - `ReadFrom()`
- Sent ICMP Echo Requests to destination hosts
- Received ICMP Echo Replies
- Added RTT (Round Trip Time) measurements using:
  - `time.Now()`
  - `time.Since()`
- Implemented timeout handling using:
  - `SetReadDeadline()`
- Added multiple probes using a loop
- Added sequence number tracking for probes
- Parsed ICMP packets using:
  - `icmp.ParseMessage()`
- Learned how ICMP replies are validated using:
  - process IDs
  - sequence numbers
- Began modularizing the project into:
  - `main.go`
  - `latencyTool.go`
  - `pingTool.go`

### What I learned

- Difference between TCP latency testing and raw ICMP networking
- How ICMP Echo Requests and Echo Replies work internally
- How raw ICMP sockets operate at the packet level
- Why raw sockets usually require administrator privileges
- How RTT is measured using send and receive timestamps
- How ICMP packet parsing works
- Why sequence numbers help identify individual probes
- Why process IDs help validate packet ownership
- Difference between generic ICMP message bodies and Echo Reply structures
- How Go type assertions work:
  ```go
  rm.Body.(*icmp.Echo)

## May 19, 2026

### What I did

- Continued improving the ICMP ping tool
- Added:
  - average RTT calculation
  - minimum RTT tracking
  - maximum RTT tracking
  - packet loss calculation
- Added validation for:
  - ICMP Echo Reply IDs
  - sequence numbers
- Improved timeout handling logic
- Added probe delays using:
  ```go
  time.Sleep()
  ```
- Cleaned up packet parsing and output formatting
- Refactored the project architecture into reusable modules
- Shared host input between:
  - latency tool
  - ping tool
- Successfully tested the tool against:
  - `google.com`
- Used:
  ```bash
  sudo go run .
  ```
  to run the complete networking toolkit

### What I learned

- How ping statistics are calculated
- Difference between:
  - average RTT
  - minimum RTT
  - maximum RTT
  - packet loss
- Why reply validation is important in raw socket networking
- How process IDs and sequence numbers help verify ICMP replies
- How ICMP packets are parsed into structured message types
- How Go type assertions work using:
  ```go
  rm.Body.(*icmp.Echo)
  ```
- Why raw ICMP networking typically requires administrator privileges
- How Go packages/functions interact across multiple files

### Problems

- Function definitions and function calls did not initially match
- `go run main.go` only compiled a single file instead of the full package
- Empty Go files caused package compilation errors
- RTT statistics initially failed due to variable scope issues
- Timeout logic was initially checked in the wrong location

### Fixes

- Matched function definitions with function calls
- Switched from:
  ```bash
  go run main.go
  ```
  to:
  ```bash
  go run .
  ```
- Added:
  ```go
  package main
  ```
  to all project files
- Moved statistics variables outside probe loops
- Corrected timeout handling placement around:
  ```go
  ReadFrom()
  ```

### Current Status

- TCP latency tool is functional
- ICMP ping tool is functional
- RTT statistics are working
- Packet loss tracking is working
- Sequence validation is implemented
- Project structure has been modularized

### Next Steps

- Begin implementing TTL manipulation
- Handle ICMP Time Exceeded messages
- Start hop-by-hop traceroute discovery
```